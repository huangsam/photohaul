# Migrator mode
migrator.mode=PATH

# Source path on local to visit photos
path.source=Dummy/Source

# Number of threads to use for parallel migration (default: 1)
# migration.threads=1

# Fallback folder name to use when metadata is missing (default: Other)
# folder.fallback=Other
